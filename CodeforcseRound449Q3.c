#include<stdio.h>
#include <string.h>
void recursion();
int main()
{
	int q;
	scanf("%d",&q);
	while (q>0)
	{

		q--;
	}
}
int temp;
void recursion(int n)
{
	if (n==1)
	{
		char arr[] =  "What are you doing at the end of the world? Are you busy? Will you save us?";
		temp=strlen(char);
	}
	else 
	{
		char arr[strlen(char)+69]
	}
