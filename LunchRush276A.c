#include <stdio.h>
int n;
long long k;
int main()
{
	scanf("%d %lld",&n,&k);
	long long fwaaladata[n],twaaladata[n];
	for (i=0;i<n;i++)
		scanf("%lld %lld",&fwaaladata[i],&twaaladata[i]);
	return 0;
}